name: Mirror to GitLab

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  mirror-disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Mirror temporarily disabled
        run: echo "GitLab mirroring is disabled until Cloudflare Access service authentication is configured."
